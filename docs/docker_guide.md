## WSL2 Ubuntu 환경에서 Docker 패키지 설치 방법
1. 윈도우 검색 탭에서 powershell을 검색 후 관리자 권한을 으로 실행하세요.
2. powershell에서 WSL2 설치
   ```
   wsl --install
4. 재부팅 후 
5. powershell에서 WSL2 설치 확인
   ``` 
   wsl -l -v
## Docker 서비스 실행 방법

## docker --version 확인 방법

## sudo 없이 Docker 명령어 사용하는 설정

## 간단한 테스트 컨테이너 실행 방법
